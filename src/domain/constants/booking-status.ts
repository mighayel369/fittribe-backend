export enum BOOKING_STATUS {
    PENDING = "pending",
    CONFIRMED = "confirmed",
    COMPLETED = "completed",
    CANCELED = "cancelled",
    RESCHEDULE_REQUESTED = "reschedule_requested",
    REJECTED = 'rejected'
}
